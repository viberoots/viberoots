# Buck2 targets

load("@prelude//genrule:genrule.bzl", "genrule")

genrule(
    name = "scaffolding",
    srcs = ["tools/tests/scaffolding.test.ts"],
    out = "scaffolding.stamp",
    cmd = "node --experimental-strip-types --test tools/tests/scaffolding.test.ts && echo ok > $OUT",
    type = "test",
)
