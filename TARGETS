# Buck2 targets
load("//tools/tests:defs.bzl", "auto_zx_tests")

# Auto-generate one zx_test per matching test file.
auto_zx_tests(
    root = "tools/tests",
    patterns = [
        "**/*.test.ts",
        "e2e-provider-wiring.ts",
    ],
)

