load("@prelude//:rules.bzl", "export_file")

export_file(
    name = "project-enforcement-runner.ts",
    src = "project-enforcement-runner.ts",
    visibility = ["PUBLIC"],
)

[
    export_file(
        name = runner,
        src = runner,
        visibility = ["PUBLIC"],
    )
    for runner in glob(["*.project-enforcement.test.ts"])
]
