load("@prelude//:rules.bzl", "filegroup")

filegroup(
    name = "runtime_ts",
    srcs = glob([
        "**/*.ts",
    ], exclude = [
        "**/.export-cache/**",
    ]),
    visibility = ["PUBLIC"],
)
