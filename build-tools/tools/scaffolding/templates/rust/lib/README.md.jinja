# {{ name }}

This Rust library uses checked-in Cargo metadata and the viberoots Nix toolchain.

Run `u` after changing `Cargo.toml`, then use `i`, `b //projects/libs/{{ name }}:{{ name }}`,
and `v //projects/libs/{{ name }}:{{ name }}-test`. Libraries are not runnable with `p` or `d`.
