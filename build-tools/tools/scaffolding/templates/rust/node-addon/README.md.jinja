# {{ name }}

This Node-API addon is implemented in Rust and built with the viberoots Nix toolchain.

Run `u` after changing `Cargo.toml`, then use `i` and
`b //projects/libs/{{ name }}:{{ name }}`. Addon libraries are not runnable with `p` or `d`.
