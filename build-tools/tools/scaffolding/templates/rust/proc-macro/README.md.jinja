# {{ name }}

This Rust procedural macro uses checked-in Cargo metadata and the viberoots Nix toolchain.

Run `u` after changing `Cargo.toml`, then use `i` and
`b //projects/libs/{{ name }}:{{ name }}`. Procedural-macro libraries are not runnable with
`p` or `d`.
