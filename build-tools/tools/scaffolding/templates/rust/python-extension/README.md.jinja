# {{ name }}

This Python extension is implemented in Rust and built with the viberoots Nix toolchain.

Run `u` after changing `Cargo.toml`, then use `i` and
`b //projects/libs/{{ name }}:{{ name }}`. The build imports the installed Python module as
its smoke test; extension libraries are not runnable with `p` or `d`.
