[package]
name = "{{ name }}-app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
{{ name | replace("-", "_") }}_core = { package = "{{ name }}-core", path = "../{{ name }}-core", version = "0.1.0" }
