# {{ name }}

This is an experimental Rust CLI built from checked-in Cargo metadata with the viberoots Nix
toolchain.

Run `u` after changing `Cargo.toml`, then use `i`, `b`, `v`, and `p`.
Package-local dependency patches are stored under `patches/rust/`.
