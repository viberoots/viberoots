# {{ name }}

This package builds both browser-style WebAssembly and WASI artifacts with the viberoots Nix
toolchain.

Run `u` after changing `Cargo.toml`, then use `i`,
`b //projects/libs/{{ name }}:{{ name }}`, and
`b //projects/libs/{{ name }}:{{ name }}-wasi`. Use `p` or `d` only with runnable targets.
