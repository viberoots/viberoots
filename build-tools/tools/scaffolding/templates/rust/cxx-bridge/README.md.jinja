# {{ name }}

This scaffold emits separate public Rust/C FFI (`{{ name }}-c`) and Rust/C++ bridge
(`{{ name }}`) targets built with the compatible viberoots Nix toolchains.

Run `u` after changing `Cargo.toml` or `bindings.json`, then use `i` and
`b //projects/libs/{{ name }}:{{ name }}`. Bridge libraries are not runnable with `p` or `d`.
