# {{ name }} Cloudflare Pages Deployment

This package publishes a reviewed static webapp artifact to Cloudflare Pages.

Keep `wrangler.jsonc` limited to provider-native Wrangler settings. Deployment identity, lane
policy, preview posture, admission policy, and requirement metadata remain authoritative in
`TARGETS`.
