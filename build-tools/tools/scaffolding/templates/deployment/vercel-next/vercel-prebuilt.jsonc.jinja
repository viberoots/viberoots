{
  "schemaVersion": "vercel-prebuilt-publisher@1",
  "team": "{{ team }}",
  "project": "{{ project }}",
  "environment": "{{ environment }}",
  "artifact": "{{ component }}",
  "canonicalUrl": "{{ canonical_url }}"
}
