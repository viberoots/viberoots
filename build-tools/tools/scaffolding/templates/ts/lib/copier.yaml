version: 1

_envops:
  block_start_string: "{%"
  block_end_string: "%}"
  variable_start_string: "{{"
  variable_end_string: "}}"
  comment_start_string: "{#"
  comment_end_string: "#}"
  keep_trailing_newline: true

_exclude:
  - meta.json

name: ""
language: "ts"
template: "lib"

importer: "projects/libs/{{ name }}"
lockfilePath: "projects/libs/{{ name }}/pnpm-lock.yaml"
pkgScope: "@libs"
includeNodeTests: true

_tasks:
  - mkdir -p projects/libs/{{ name }}/src
  - mkdir -p projects/libs/{{ name }}/test
