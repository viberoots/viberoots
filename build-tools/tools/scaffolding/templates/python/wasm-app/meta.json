{
  "language": "python",
  "template": "wasm-app",
  "description": "Python WASM app scaffold (WASI default; optional Pyodide)",
  "help": {
    "usage": "scaf new python wasm-app <name> [--path=DEST]",
    "notes": [
      "Creates projects/apps/<name> with uv.lock and TARGETS using nix_python_wasm_app.",
      "Default backend is wasi; add label backend:pyodide to switch.",
      "Lockfile label uses importer-scoped form: lockfile:projects/apps/<name>/uv.lock#projects/apps/<name>."
    ],
    "examples": [
      "scaf new python wasm-app demo-wasm --path=projects/apps/demo-wasm",
      "scaf regen projects/apps/demo-wasm"
    ]
  }
}
