# {{ name }} (Python WASM Lib)

This is a minimal Python WASM library scaffold. It produces a reusable site overlay
that WASM apps can consume.

## Usage

Build through the canonical Buck artifact route:

```bash
b //projects/libs/{{ name }}:{{ name }}
```

Consume the resulting overlay through a declared Buck dependency from the application target.

