load("@prelude//:rules.bzl", "export_file")

# Expose overlay source as a Buck target so it can be referenced by label.
export_file(
    name = "cpp-patches.nix",
    src = "cpp-patches.nix",
    visibility = ["PUBLIC"],
)

