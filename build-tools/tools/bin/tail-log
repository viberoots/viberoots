#!/usr/bin/env bash
set -euo pipefail

# shell helpers
# shellcheck source=/dev/null
. "$(cd "$(dirname "$0")" && pwd)/_env.sh"

# Substantive logic lives in TypeScript (zx). Keep this wrapper tiny for file-size linting.
run_ts "../dev/tail-log.ts" "$@"
