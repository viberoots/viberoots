export const validRuntimeInventory = `
{
  allowedPrimitives = [
    "kernel-sandbox-support"
    "disk-capacity"
    "network-reachability"
    "mounted-credentials-or-workload-identity"
    "trust-anchors"
    "clock"
    "minimal-nix-bootstrap"
  ];
}
`;
