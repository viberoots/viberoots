#!/usr/bin/env zx-wrapper
import { registerProtectedRustPatchLocalDaemonIntegration } from "./protected-rust-patch-local-daemon.integration.fixture";

registerProtectedRustPatchLocalDaemonIntegration(0);
