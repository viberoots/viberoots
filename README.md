# demo-lib (Go library)

Generated by scaf (template: go/lib).

Build after glue generation:

```
build
verify
```

Providers: nix*go*\* macros attach providers automatically via `third_party/providers/auto_map.bzl`.
Glue is generated locally by the build wrappers (and in CI via stages).

See docs in `docs/handbook/` for end-to-end usage and workflows.
CI stage runner reference: `tools/ci/run-stage.ts`.
