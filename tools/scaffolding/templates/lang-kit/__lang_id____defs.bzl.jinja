# //{{ lang_id }}/defs.bzl — skeleton macros
# Generated by lang-kit. Adjust loads to your language rules.

def _providers_for(name):
    MODULE_PROVIDERS = {}
    load("//third_party/providers:auto_map.bzl", "MODULE_PROVIDERS")
    pkg = native.package_name()
    key = "//%s:%s" % (pkg, name)
    return MODULE_PROVIDERS.get(key, [])

# Example passthrough macros namespaced by lang id; replace with real rules

def {{ lang_id }}_library(name, **kwargs):
    deps = kwargs.pop("deps", []) + _providers_for(name)
    native.export_file(name = name, src = "BUILD.bazel", visibility = ["//visibility:public"])  # placeholder


def {{ lang_id }}_binary(name, **kwargs):
    deps = kwargs.pop("deps", []) + _providers_for(name)
    native.export_file(name = name, src = "BUILD.bazel", visibility = ["//visibility:public"])  # placeholder
