document.addEventListener("DOMContentLoaded", async () => {
  const root = document.getElementById("app")!;
  root.innerHTML = `
    <h1>{{ name }} (WASM Demo)</h1>
    <p>Open the console to see results.</p>
  `;
  console.log("WASM app scaffold ready.");
});


