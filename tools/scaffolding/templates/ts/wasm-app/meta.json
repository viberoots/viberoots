{
  "language": "ts",
  "template": "wasm-app",
  "description": "Minimal browser app scaffold that can load a Wasm module and call add()",
  "help": {
    "usage": "scaf new ts wasm-app <name> [--path=DEST]",
    "notes": [
      "Creates apps/<name> with a tiny webapp skeleton. You can wire it to a library",
      "generated by the ts/go-cpp-lib scaffold or point it at any top.wasm."
    ],
    "examples": ["scaf new ts wasm-app demo"]
  }
}
