node-linker=isolated
patches-dir=patches/node

node-linker=isolated
patches-dir=patches/node

# Ensure pnpm does not try to self-bootstrap via corepack in hermetic builds
package-manager-strict=false

