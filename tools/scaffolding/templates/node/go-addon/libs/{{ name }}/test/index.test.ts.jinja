import { describe, it, expect } from "vitest";
import { add } from "../src/index.ts";

describe("native addon add", () => {
  it("computes 2 + 3 = 5", () => {
    expect(add(2, 3)).toBe(5);
  });
});


