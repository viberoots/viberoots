import { describe, it, expect } from "vitest";
import { sum } from "../src/index.ts";

describe("native addon sum", () => {
  it("computes 2 + 40 = 42", () => {
    expect(sum(2, 40)).toBe(42);
  });
});


