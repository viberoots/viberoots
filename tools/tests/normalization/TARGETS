load("//lang:defs_common.bzl", "normalize_nix_attr_probe")

# Starlark probes for normalization parity checks
normalize_nix_attr_probe(name = "case1", attr = "gtest")
normalize_nix_attr_probe(name = "case2", attr = "pkgs.gtest")
normalize_nix_attr_probe(name = "case3", attr = "pkgs.openssl")
normalize_nix_attr_probe(name = "case4", attr = "pkgs.gnome.glib")



