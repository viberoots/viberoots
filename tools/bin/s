#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Status watch for verify logs.
# Equivalent to: tools/bin/tail-log --status -w [SECONDS] [--json] [PID]
exec "${SCRIPT_DIR}/tail-log" --status -w "$@"


