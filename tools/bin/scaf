#!/usr/bin/env bash
set -euo pipefail
# shell helpers
# shellcheck source=/dev/null
. "$(cd "$(dirname "$0")" && pwd)/_env.sh"

env_init_paths "$0"
TARGET="${LIVE_ROOT}/tools/scaffolding/scaf.ts"

ensure_coverage_dir "${REPO_ROOT}"

node_ts "${LIVE_ROOT}" "${TARGET}" "$@"
