load("@prelude//:rules.bzl", "filegroup")

filegroup(
    name = "migrations",
    srcs = glob(["migrations/*.sql"]),
    labels = ["kind:migrations", "deployment:migration-set"],
    visibility = ["//projects/deployments/..."],
)
