load("//node:defs.bzl", "nix_node_test")

nix_node_test(
    name = "provider_stamp",
    srcs = [],
    out = "provider.stamp",
    cmd = "echo provider_ok > $OUT",
    lockfile_label = "lockfile:apps/example/pnpm-lock.yaml#apps/example",
)

nix_node_test(
    name = "smoke_test",
    srcs = [],
    out = "test.stamp",
    cmd = "node -e 'console.log(\"ok\")' && echo ok > $OUT",
    lockfile_label = "lockfile:apps/example/pnpm-lock.yaml#apps/example",
)


