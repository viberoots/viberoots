{
  "name": "@apps/demo-node",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "bin": {
    "demo-node": "bin/demo-node"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test dist/**/*.test.js",
    "lint": "eslint . --ext .ts",
    "format": "prettier -w ."
  }
}
